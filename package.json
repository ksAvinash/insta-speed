{
  "name": "insta-speed",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:mobile": "vite --host",
    "build": "npm run models && vite build",
    "preview": "vite preview",
    "test": "node --test",
    "models": "node scripts/generate-vehicle-glbs.mjs"
  },
  "dependencies": {
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@vitejs/plugin-basic-ssl": "^2.1.0",
    "vite": "^8.2.0"
  }
}
