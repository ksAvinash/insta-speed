# Cloudflare Web Analytics site token (optional).
# Dashboard → Analytics & logs → Web Analytics → Add a site → JS snippet.
# For GitHub Pages, also add this as Actions secret `VITE_CF_BEACON`.
VITE_CF_BEACON=
